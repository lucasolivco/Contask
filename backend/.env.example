# Desenvolvimento local apenas
# Desenvolvimento sem docker

EXPIRES_IN="7d"

# Server
PORT=3001
# NODE_ENV="development"

# AMBIENTE
NODE_ENV=development
DEBUG=true

# PORTAS
BACKEND_PORT=3001
FRONTEND_PORT=5173
DB_PORT=5432
REDIS_PORT=6379

# DATABASE (PostgreSQL local via Docker)
# DATABASE - ALTERAR SENHAS!
DB_USER=contask_user
DB_PASSWORD=ALTERAR_SENHA_FORTE_32_CARACTERES
DB_NAME=contask_production

# REDIS - ALTERAR SENHA!
REDIS_PASSWORD=ALTERAR_SENHA_FORTE_32_CARACTERES

# JWT SECRETS - GERAR CHAVES FORTES!
JWT_SECRET=GERAR_CHAVE_FORTE_64_CARACTERES
JWT_REFRESH_SECRET=GERAR_OUTRA_CHAVE_FORTE_64_CARACTERES

DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public&sslmode=disable

# REDIS (local)
REDIS_URL=redis://:${REDIS_PASSWORD}@postgres:${REDIS_PORT}

#grafana
GF_SECURITY_ADMIN_PASSWORD=

# SMTP (teste local com Mailtrap ou similar)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-do-app

# URLs
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001

# UPLOAD
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# RATE LIMITING (mais permissivo em dev)
RATE_LIMIT_WINDOW=
RATE_LIMIT_MAX=
RATE_LIMIT_AUTH_MAX=

MAX_FILE_SIZE=
UPLOAD_PATH=/app/uploads